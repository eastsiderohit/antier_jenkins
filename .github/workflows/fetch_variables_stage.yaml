name: Fetch Variables for Stage Branch

on:
  push:
    branches:
      - stage

jobs:
  fetch-variables:
    runs-on: ubuntu-latest

    outputs:
      prod-repository: stage-shido-jenkins
      ecr-endpoint: 096085577054.dkr.ecr.eu-west-1.amazonaws.com
      ecr-push-command: 'aws ecr get-login-password --region eu-west-1 | docker login --username AWS --password-stdin 096085577054.dkr.ecr.eu-west-1.amazonaws.com'
